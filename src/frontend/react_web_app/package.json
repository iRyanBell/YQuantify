{
  "name": "yquantify-frontend-react",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "prettier --write \"src/**/*.js\"",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postbuild": "rm -rf ../../../public && mv build ../../../public"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
