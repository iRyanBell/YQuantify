import React from "react";
import Layout from "../../layout/Layout";
import CheckoutForm from "./CheckoutForm";

export default () => {
  return (
    <Layout>
      <CheckoutForm />
    </Layout>
  );
};
